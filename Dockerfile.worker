# AI Worker - separate ECS Task definition (2 vCPU / 4 GB, GPU Spot for Phase 2+)
FROM python:3.11-slim AS base

WORKDIR /app

RUN pip install --no-cache-dir uv

COPY pyproject.toml .
RUN uv pip install --system --no-cache -e "."

COPY . .

CMD ["python", "-m", "worker.consumer"]
